# Docker build environment variables
# These are placeholder values for the Docker build process

# Supabase Configuration (placeholders for build)
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-anon-key
SUPABASE_SERVICE_ROLE_KEY=placeholder-service-key

# Database Configuration (placeholders for build)
DATABASE_URL=postgresql://user:pass@localhost:5432/db

# Google Cloud Configuration (placeholders for build)
GOOGLE_CLOUD_PROJECT_ID=placeholder-project
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials.json
GOOGLE_CLOUD_STORAGE_BUCKET=placeholder-bucket

# Pinecone Configuration (placeholders for build)
PINECONE_API_KEY=placeholder-pinecone-key
PINECONE_INDEX_NAME=placeholder-index

# Redis Configuration (placeholders for build)
UPSTASH_REDIS_REST_URL=redis://localhost:6379
UPSTASH_REDIS_REST_TOKEN=placeholder-redis-token

# Processing Configuration
UNLIMITED_PROCESSING=false
VERBOSE_LOGS=false

# Application Configuration
NODE_ENV=production
NEXTAUTH_SECRET=placeholder-secret
NEXTAUTH_URL=http://localhost:3000